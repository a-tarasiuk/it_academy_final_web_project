package by.tarasiuk.ct.model.entity;

import java.io.Serializable;

/**
 * This empty class is created to further verify classes using "instanceof"
 * to determine whether objects belong to the designed project entities.
 * The class doesn't contain fields of the ID, since not all tables contain
 * this field.
 */

public interface Entity extends Serializable, Cloneable {
}
